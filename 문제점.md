개발자 화면 말고 다른거도 화면공유할때 안나오는게 있을까 ?
코드리뷰 하거나 할때 

개선점
함수 부분 실행 수정하면 좋을듯
// Case 1: Nested zero-arg function (Should return 100)
const deepFunction = () => () => () => 100;

// Case 2: Function with arguments (Should show source code)
const add = (a, b) => a + b;

console.log("Add function source:", add);
return deepFunction;
이거 return쓰면 잘 보여주는데(100출력됨)
이거 그냥 return 없이 
// Case 1: Nested zero-arg function (Should return 100)
const deepFunction = () => () => () => 100;

// Case 2: Function with arguments (Should show source code)
const add = (a, b) => a + b;

console.log("Add function source:", add);
deepFunction();
이렇게 만 해도return deepFunction; 이렇게 할수있게끔 수정

객체 안에 값은 object 이부분 개선!
이거 
const formatOutput = (target) => {
  if (target === undefined) return "undefined";
  if (target === null) return "null";
  
  // Use JSON.stringify for objects and arrays to see their contents
  if (typeof target === 'object') {
    return JSON.stringify(target, null, 2); // 2 spaces for indentation
  }
  
  return String(target);
}
이렇게 undefind 로나와서 나이거 수정해야함

tap 지원! 포커싱 이동 막고 -> 성공
strict 모드에서 가능하게 옵션추가 -> 첨에 키고 실행 할때는 되는데 안키고 실행 후 그상태로 키고 사용하면 안키고 실행한 버전이 실행됨

